# candidate_outputs/

Per-candidate forecast tables that conform to `../experiment_contract.csv`.

- One CSV per candidate (e.g. `FNAR-V2_forecasts.csv`), long/tidy format.
- Written by the V3.2C harness ONLY. Empty until V3.2C runs.
- These are EXPERIMENTAL artifacts. They are NEVER written to `data/processed/`.
- No file here changes the governed champion, forecasts, intervals, or governance decisions.
